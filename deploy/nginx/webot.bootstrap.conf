server {
    listen 80;
    listen [::]:80;
    server_name webot.geneyuriy.com;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 503 "webot deployment in progress\n";
        add_header Content-Type text/plain;
    }
}
